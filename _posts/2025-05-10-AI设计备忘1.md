---
layout: post
title: AI设计备忘1
header-img: img/post-bg-universe.jpg
catalog: true
tags: [设计]
author: null
subtitle: null
---

完整总结 + 实施步骤如下（按你最终口径）：

一、最终统一口径
规则来源

统一来自 geDomainModel 返回的元数据：*FieldMeta（如 TextFieldMeta）+ Meta 基类。
默认值、默认长度、校验约束都由 Meta 提供（可扩展属性）。
既有运行时定义

后端运行时/领域对象以既有定义为准：TextField + 基类（你们现有体系）。
保存不是“前端传啥存啥”，而是后端转领域对象后再落库。
保存阶段边界

按 Meta 做默认值补齐（仅缺失补，不覆盖用户输入）。
做输入合法性校验（例如最大长度、格式、范围、必填、引用存在性）。
最终序列化 JSON 持久化到 md_entity_definition。
分层

entity：语义字段（不放建表约束）
table：物理建表字段（放 Unique/Indexed/PrimaryKey）
即：
entity 不要 Unique/Indexed/PrimaryKey
table 要 Unique/Indexed/PrimaryKey
前端职责

仅输入与展示，不写死业务默认规则。
保存前只做结构规范化与必要兜底提示，不定义业务规则真相。
二、你当前问题的根结（已对齐）
历史 Items/entity 双结构不一致导致删改回滚、字段“复活”
前端曾写死默认逻辑与空串处理，和后端校验口径冲突
列表/过滤绑定字段空值导致后端报错
三、实施步骤（按优先级）
P0（先落地，止血）
后端保存入口统一规范化
入参先 normalize（trim、空值处理、结构对齐）。
entity/table 分离处理
entity 剔除 Unique/Indexed/PrimaryKey
table 保留 Unique/Indexed/PrimaryKey
返回 4xx 业务错误而不是 500
包含字段路径，便于前端定位。
P1（核心改造）
接入 Meta 驱动
用 geDomainModel 的 *FieldMeta + 基类Meta 作为默认/校验来源。
映射到既有定义对象
字段映射到你们既有 TextField + 基类对象体系。
保存链路
payload -> meta补默认 -> 合法性校验 -> 序列化 -> md_entity_definition
P2（收敛与清理）
前端移除硬编码默认规则
不再写死长度/默认值。
只保留必要 payload
去掉运行态冗余字段（Type/Type/等非必要项）。
读写口径统一
页面回显以 md_entity_definition 反序列化结果为准。
四、我接下来“马上要做”的事（执行清单）
清理前端保存前硬编码默认逻辑（长度/默认值推断全部去掉）。
保留前端最小结构规范化（只做字段裁剪与引用必填兜底提示）。
对齐 entity/table payload 白名单：
entity 不发 Unique/Indexed/PrimaryKey
table 继续发这三项。
在保存失败提示中展示后端返回的字段路径（可定位到具体列）。
为你准备后端改造接口契约草案（geDomainModel 规则字段映射表 + 保存校验返回结构）。


以 geDomainModel 返回的 *FieldMeta + Meta基类 为唯一规则源，后端将前端输入转换为既有领域对象（TextField + 基类），完成“补默认 + 合法性校验”，最后序列化落 md_entity_definition。

一、执行分阶段方案
阶段1：规则统一（Meta 契约落地）
明确 geDomainModel 返回字段清单（每个 FieldMeta 至少包含）：
默认值：defaultValue
默认长度：defaultLength
校验约束：maxLength / regex / range / required / enumOptions ...
可扩展：validators[]、normalizers[]
定义“基类Meta + 子类Meta”的合并规则：
共性取基类
子类覆盖冲突项
输出 EffectiveMeta（运行时使用）
固化 Meta 版本号策略（防止前后端缓存错配）。
交付物：Meta字段字典、合并规则文档、版本策略。

阶段2：保存链路重构（后端）
保存入口改为“转换驱动”：
输入 UI payload
转换为既有领域对象（TextField + 基类）
不再直接透传前端结构入库
保存边界明确：
仅缺失补默认（不覆盖用户输入）
合法性校验仅按 Meta 规则执行（长度/格式/范围/必填/引用存在性）
持久化唯一真源：
领域对象序列化后存 md_entity_definition
其他运行态表仅作编辑态数据，不作为定义真源
交付物：保存流程图、校验清单、存储清单。

阶段3：数据分层治理（entity/table）
entity 只保留语义字段（不放建表约束）
table 保留物理约束（Unique/Indexed/PrimaryKey）
保存前后统一清洗策略：
entity 中若出现物理约束字段，剔除/忽略并记录告警
table 中必须保留物理约束字段
交付物：entity/table 字段白名单矩阵。

阶段4：错误模型与可观测性
统一返回 4xx 业务校验错误（不抛 500）
错误体必须含：
错误码
字段路径
失败原因
增加保存链路观测点：
Meta版本
默认补齐数量
校验失败分布
交付物：错误码规范、监控指标清单。

阶段5：前端对齐（最小职责）
前端移除硬编码默认逻辑（长度/默认值）
前端只做输入展示与基础格式提示
保存后以 md_entity_definition 回读结果回显
交付物：前端职责边界文档、回显一致性验收项。

二、关键规则（必须固化）
规则真源：仅 geDomainModel 的 *FieldMeta
运行时真源：既有领域对象（TextField + 基类）
落库真源：md_entity_definition
默认补齐原则：仅缺失补，不覆盖用户值
校验原则：合法性校验，不做额外业务推断
三、验收标准（你可以直接拿去评审）
同一字段在不同入口保存，默认长度/默认值一致（由 Meta 驱动）
前端不传长度时，后端按 Meta 自动补齐
前端传了长度时，后端不覆盖，仅校验是否合法
非法输入返回 4xx，且能定位到具体字段路径
删除/切页/保存后，回读 definition 与页面展示一致
entity 不含 Unique/Indexed/PrimaryKey，table 保留这些约束
四、你接下来要排的任务（建议顺序）
先定 Meta 契约与合并规则（最优先）
改保存链路为“先转领域对象再落库”
接入默认补齐与合法性校验
接入 4xx 错误模型
前端去硬编码并改为 definition 回读


Epic: Entity Definition Meta-Driven Save Pipeline
Story 1：接入 geDomainModel 元数据
Task 1.1：定义 Meta 读取接口（按 formId）
Task 1.2：实现 BaseMeta + 子类Meta 合并（EffectiveMeta）
Task 1.3：加本地缓存与版本标识（防止频繁拉取）
验收：给定 TextFieldMeta，能拿到包含基类属性的 EffectiveMeta。

Story 2：保存链路领域化
Task 2.1：保存入口 payload normalize
Task 2.2：payload 映射到既有领域对象（TextField + 基类）
Task 2.3：禁止“前端字段直存”，统一经领域对象再序列化
验收：保存后 md_entity_definition 中的数据来源于领域对象序列化结果。

Story 3：默认值补齐（仅缺失补）
Task 3.1：按 EffectiveMeta 补 defaultValue/defaultLength
Task 3.2：补齐时不覆盖用户已输入值
Task 3.3：记录补齐明细日志（字段、来源meta）
验收：未填长度时自动补；已填长度时保持原值。

Story 4：输入合法性校验
Task 4.1：长度上限校验（来自 Meta）
Task 4.2：格式/范围/必填校验（来自 Meta）
Task 4.3：引用存在性校验（list/filter binding）
验收：非法输入返回业务错误，不写库。

Story 5：entity/table 分层规则
Task 5.1：entity 层剔除 Unique/Indexed/PrimaryKey
Task 5.2：table 层保留并校验 Unique/Indexed/PrimaryKey
Task 5.3：冲突场景给出明确错误信息
验收：entity 不落物理约束，table 保留物理约束。

Story 6：错误模型标准化
Task 6.1：统一异常为 400（Validation）
Task 6.2：错误体包含 code/message/details[path]
Task 6.3：保留 traceId 便于排查
验收：不再出现当前这类 500 IllegalArgumentException 外抛。

Story 7：回读一致性
Task 7.1：保存后读取 md_entity_definition 作为回显真源
Task 7.2：设计器加载以 definition 反序列化结果为准
Task 7.3：兼容旧数据迁移（Items/entity 混合）
验收：切页/刷新/重进后字段不丢不复活。

联调约束（你可直接拍板）
前端不再写死默认长度/默认值
后端是唯一规则真源（Meta + 领域定义）
任何校验失败必须返回可定位 path
落库真源唯一：md_entity_definition

Meta 提供层（geDomainModel）
按 vendorCode 输出整套模型（不只是某个字段类型）
包括基类Meta + 各子类Meta + 校验约束扩展属性
Meta 合并与解析层
基类/子类合并器（EffectiveMeta）
厂商覆盖策略（同 key 覆盖、数组合并策略）
保存规范化层
entity/table 白名单标准化
厂商级 normalize hook（全量模型可改）
默认补齐层
按 EffectiveMeta 对所有字段类型补默认（仅缺失补）
合法性校验层
按 Meta validators 校验（长度/范围/格式/必填/引用）
返回结构化错误 path
持久化映射层
payload -> 既有领域对象（TextField + 基类体系）
definition 序列化 -> md_entity_definition
扩展插件层
DomainModelVendorCustomizer（元模型输出定制）
EntityMetaVendorCustomizer（保存链路定制）
后续新增厂商只加插件，不改主干

具体方案如下（按你的 A 标准 / B 扩展模式）：

1. 总体架构
Base Model（A）：平台标准元数据模型
Vendor Overlay（B）：在 A 上增量扩展
Effective Model：运行时合并结果，供设计器与保存链路统一使用
合并公式：Effective = Base + VendorOverlay (+ FormOverlay 可选)

2. 元数据扩展内容（B 可以扩什么）
字段扩展
新增字段类型
给已有字段类型增加属性（如“简字段”标记）
默认值扩展
覆盖 defaultLength/defaultValue/nullableDefault
校验扩展
maxLength、regex、range、requiredWhen
业务逻辑扩展
保存前规则（pre-save normalize）
保存时校验（validate hooks）
保存后处理（post-save hook，可选）
3. 合并规则（必须固定）
基类属性先加载，子类覆盖
VendorOverlay 同 key 覆盖 Base
数组类（validators）按 id 合并（同 id 覆盖，不同 id 追加）
不允许删除 Base 必需字段，只允许覆盖/追加（可配置白名单放开）
4. 保存链路（后端）
读取 tenant -> vendorCode
取 Base DomainModel
取 VendorOverlay（B）
构建 EffectiveModel
payload 标准化（entity/table 分层）
按 EffectiveModel 补默认（仅缺失补）
按 EffectiveModel 校验合法性
映射到既有领域对象（TextField + 基类）
序列化落库 md_entity_definition
返回统一 4xx 错误格式（含 path）
5. 扩展点接口（你们实现层）
DomainModelVendorCustomizer：元数据模型扩展
EntityMetaVendorCustomizer：保存链路扩展
VendorValidationProvider（建议补）：厂商业务校验插件
6. A/B 示例语义
A（标准）：
TextField 长度默认 255
B（扩展）：
基础资料表单新增“简字段”
TextField 默认长度改为 128
新增规则：简字段仅允许字母数字下划线
保存时增加 B 专属字段映射
7. 交付顺序
先落 Overlay 数据结构与合并器
再接保存链路用 EffectiveModel
再补 B 示例扩展（字段+校验+hook）
最后加回归用例（A 不受影响，B 生效）


纠正后的方案（客户零代码）
1) 平台内核固定（你们维护）
TextFieldMeta / BaseFieldMeta 等作为内核基模
提供“元数据扩展解释器”（Overlay Engine）
不要求客户写 Java 类
2) 客户扩展以“配置模型”交付
客户只上传/维护配置（DB/JSON），例如：

vendor_domain_model_overlay（按租户/厂商）
category overlay
element overlay
property overlay
vendor_validation_rules
maxLength、regex、range、requiredWhen
vendor_default_rules
defaultValue、defaultLength（仅缺失补）
vendor_save_hooks（可选，声明式，不执行客户代码）
3) 运行时拼装
请求 geDomainModel 时： Base Meta + Tenant/Vendor Overlay + Form Overlay => Effective Meta

保存时： payload -> normalize -> Effective Meta 补默认 -> Effective Meta 校验 -> 持久化(md_entity_definition)

关键收益
客户不接触源码，不需要发版
多租户差异可配置、可回滚、可审计
你们平台仍保持主干可控
你现在应做的改造点
把我刚做的 VENDOR_B 代码示例，迁移为“配置驱动”
保留扩展引擎，但扩展来源改为 DB/JSON（不是 Java 实现类）
增加后台管理能力：
导入/发布 overlay
版本管理
灰度到租户
增加安全边界：
只允许白名单属性扩展
禁止任意脚本执行
如果你同意，我下一步就给你出一版**“元数据扩展配置 schema”**（表结构/JSON结构），直接能给后端开工。

你直接回滚刚才的逻辑，我建议做成配置表，不对可以反驳我，配置表要记录是哪个厂商的扩展，从哪个厂商扩展的，扩展前的状态要保存下来，我说的状态是指完整的元数据定义，后面可能要做成界面，你先给出方案


你的思路是对的，我不反驳：这类 PaaS 能力必须配置化，不该让客户改代码。
而且你提的三个点都非常关键：

记录“哪个厂商”的扩展
记录“从哪个厂商继承/扩展”
保存“扩展前完整元数据状态”（可回滚/可做可视化）
下面给你一版可落地方案。

一、总体模型（推荐）
采用 三层模型：

Platform Base：平台内置完整元数据（全量）
Vendor Base/Overlay：厂商级扩展（增量或全量）
Tenant/Form Overlay（可选）：租户或表单级扩展
运行时生成：EffectiveMeta = Base -> ParentVendor -> Vendor -> Tenant/Form

二、核心表设计（配置表）
1) md_meta_model_version（元模型版本主表）
记录一份“可发布版本”的完整信息。

关键字段：

id
scope_type：platform|vendor|tenant|form
scope_id：如 vendorCode / tenantId / formId
model_type：ui|list|...
version_no
status：draft|published|archived
from_scope_type / from_scope_id / from_version_id（你说的“从哪个厂商扩展”）
snapshot_json（完整元数据定义快照，扩展前/后都可存）
overlay_json（增量定义）
checksum
created_by/created_at/updated_at
重点：snapshot_json 就是你说的“状态”，必须存全量。

2) md_meta_model_history（变更历史表）
每次保存都记录历史，支持审计与回滚。

关键字段：

id, model_version_id
op_type：create|update|publish|rollback
before_snapshot_json（完整）
after_snapshot_json（完整）
diff_json（可选）
operator, op_time, remark
3) md_meta_model_publish（发布记录表）
记录发布与生效范围。

关键字段：

id
model_version_id
target_scope（vendor/tenant/form）
publish_status
published_at
rollback_from_publish_id（回滚链路）
三、扩展与继承规则
每个扩展版本可指定 from_version_id（父版本）
保存时立即生成并保存：
before_snapshot_json（父链合并后的完整状态）
after_snapshot_json（应用本次 overlay 后完整状态）
发布时只发 version id，运行时从版本生成 EffectiveMeta 并缓存。
四、回滚机制（必须有）
回滚不是“反向操作”，而是切换到历史版本快照。
做法：
在 history 找目标版本的 after_snapshot_json
新建一条 draft/published 版本（op_type=rollback）
重新发布
保留完整链路可审计。
五、运行时读取策略
按优先级查找并合并：

form-level published（若有）
tenant-level published（若有）
vendor-level published（若有）
platform-level published（必有）
最终得到 effective_snapshot_json 供 geDomainModel 直接返回。

六、你后面要做界面时的支撑点（现在就埋好）
版本树（显示 from_version 继承链）
JSON Diff（before/after）
一键回滚
发布到范围（vendor/tenant/form）
灰度发布（可选）
七、对你当前仓库的处理建议
你说“先回滚刚才逻辑”——我建议：

保留“扩展点接口骨架”可以（不影响）
回滚掉 VENDOR_B 这种硬编码业务逻辑示例
改成从配置表读取 overlay 的实现（下一步）
如果你同意，我下一步就做两件事：

先把 VENDOR_B 硬编码示例回滚掉
补一版配置表 DDL + 示例 JSON（可直接给后端建表）

非常好，你这个要求是关键：厂商级 + 租户级 + 表单级都要支持。
我给你一版“可直接实施”的具体实现方案（先方案，等你确认我再执行回滚和落地）。

1）扩展层级与优先级
定义四层：

PLATFORM（平台基线）
VENDOR（厂商扩展）
TENANT（租户扩展）
FORM（表单扩展）
合并优先级（后者覆盖前者）：

PLATFORM -> VENDOR -> TENANT -> FORM

2）数据模型（核心表）
A. md_meta_profile
“扩展配置版本”主表（每条是一个版本）

关键字段：

id
scope_type：PLATFORM|VENDOR|TENANT|FORM
scope_id：如 VENDOR_B / tenant_001 / form_xxx
model_type：ui|list
status：DRAFT|PUBLISHED|ARCHIVED
base_profile_id：继承来源（你说“从哪个厂商扩展”）
overlay_json：增量定义
full_snapshot_json：完整定义快照（关键）
version_no
created_by/created_at/updated_at
B. md_meta_profile_history
历史表（审计/回滚）

id, profile_id, op_type
before_snapshot_json（完整）
after_snapshot_json（完整）
diff_json（可选）
operator, op_time, remark
C. md_meta_profile_publish
发布记录（生效边界）

id, profile_id
scope_type/scope_id/model_type
published_at, published_by
rollback_from_publish_id（回滚链）
3）Overlay JSON 结构（建议）
Json

Apply
{
  "categories": [
    {
      "id": "Field",
      "patch": { "name": "字段", "visible": true },
      "elements": [
        {
          "id": "TextField",
          "patch": { "name": "单行文本" },
          "properties": [
            {
              "key": "SimpleField",
              "op": "addOrReplace",
              "def": {
                "PropertyName": "SimpleField",
                "Name": "简字段",
                "Editor": { "type": "BOOLEAN", "default": false },
                "Group": "BASE"
              }
            }
          ]
        }
      ]
    }
  ],
  "rules": {
    "defaults": [],
    "validators": []
  }
}
4）保存与发布流程
保存草稿
读取继承链基线（base_profile_id 或按层级默认链）
计算 before_full_snapshot
应用 overlay_json 得 after_full_snapshot
入库 profile（overlay + full_snapshot）
写 history（before/after）
发布
仅允许 DRAFT -> PUBLISHED
写 publish 记录
刷新缓存（按 scope+modelType）
回滚
选历史版本 after_snapshot_json
生成新 draft（op=rollback）
发布该 draft（不覆盖旧记录）
5）运行时解析（geDomainModel）
请求进入后按上下文拿：

vendorCode
tenantId
formId
modelType
解析链：

平台发布版
vendor 发布版（若有）
tenant 发布版（若有）
form 发布版（若有）
合并后返回 EffectiveDomainModel。
并缓存 key：modelType:vendor:tenant:form:versionFingerprint

6）冲突策略（必须固定）
标量：后层覆盖前层
数组（categories/elements/properties）：按 id/key merge
删除操作：显式 op=remove（避免隐式丢失）
禁止删除平台保留关键字段（白名单控制）
7）校验策略
在“发布前”校验：

overlay schema 合法
引用目标存在（category/element/property）
类型兼容（EditorType 不冲突）
规则可执行（validator 参数完整）
保存运行时数据时：

仍按 EffectiveMeta 做“仅缺失补默认 + 合法性校验”
8）你马上能做的实施顺序
回滚硬编码厂商示例（保留扩展引擎接口）
建三张配置表（profile/history/publish）
实现 Overlay 合并器（纯配置驱动）
改 geDomainModel 走配置表解析链
改 saveRuntimeMeta 校验/默认读取 EffectiveMeta
增加发布与回滚 API
最后做可视化页面（版本树 + diff + 发布）
你确认后，我下一步就开始执行：

回滚硬编码 VENDOR_B 示例
保留并改造成“配置表驱动”骨架
先把表结构和服务接口落到 server。

snapshot_json格式是什么样子的，对于表单级别扩展的东西还是不一样的，我说的表单级别扩展指的是某个表单保存的元数据，和你理解的不一样，比如后端的TextField这些是表单的元素，扩展指的是在表单上加减字段和增加业务逻辑


你这个纠正非常关键，我理解到位了：

你说的表单级扩展不是“改 TextFieldMeta 模型定义”
而是“对某个具体表单的元数据做加减字段、加业务逻辑”
所以要分两类 snapshot：

1) 两种 snapshot（必须分开）
A. 模型级 snapshot（Meta Snapshot）
对象：TextFieldMeta / BaseFieldMeta 这类“定义模型”
用于：厂商/租户级元模型扩展
B. 表单级 snapshot（Form Snapshot）✅你重点要这个
对象：某个 formId 的完整设计数据（ui/formmeta/entitymeta/runtime相关）
用于：表单加减字段、业务逻辑扩展、回滚
你现在主要要的是 B。

2) 表单级 snapshot_json 建议格式（完整态）
建议直接存“可重建表单”的完整 JSON（不是 diff）：

Json

Apply
{
  "formId": "purchase_order",
  "modelType": "ui",
  "version": 12,
  "source": {
    "scopeType": "FORM",
    "scopeId": "purchase_order",
    "baseRef": {
      "scopeType": "VENDOR",
      "scopeId": "VENDOR_A",
      "version": 3
    }
  },
  "uiInfo": {
    "ui": { "...": "完整UI树" },
    "formmeta": { "...": "完整formmeta" },
    "entitymeta": {
      "Id": "xxx",
      "Name": "xxx",
      "entity": [ "...语义字段..." ],
      "table": [ "...物理字段..." ]
    }
  },
  "runtimeMeta": {
    "...": "运行时结构（如果你们需要单独保存）"
  },
  "bizLogic": {
    "beforeSave": {
      "rules": [ "...声明式规则..." ]
    },
    "validators": [ "...声明式校验..." ],
    "operations": [ "...操作定义..." ]
  },
  "meta": {
    "tenantId": "t1",
    "vendorCode": "VENDOR_A",
    "operator": "u1001",
    "savedAt": "2026-05-10T15:00:00Z"
  }
}
3) 表单级扩展怎么做（你要的语义）
对 form snapshot 做 overlay，不碰平台代码：

addFields: 增加字段
removeFields: 删除字段
updateFields: 改字段属性
bizLogicPatch: 增加业务逻辑（规则/校验/动作）
例如：

Json

Apply
{
  "targetFormId": "purchase_order",
  "ops": {
    "addFields": [{ "fieldName": "simple_code", "fieldType": "TextField", "length": 64 }],
    "removeFields": ["old_field_x"],
    "updateFields": [{ "fieldName": "amount", "nullable": false }],
    "bizLogicPatch": {
      "validators": [{ "id": "v_amount_positive", "expr": "amount > 0" }]
    }
  }
}
应用后生成新的 full_snapshot_json 并入历史。

4) 你担心“表单级别不一样”——正确处理
是的，表单级别必须有自己的存储与链路，建议：

md_form_extension_profile（表单扩展版本）
md_form_extension_history（前后快照）
md_form_extension_publish（发布）
不要和模型级 profile 混成一张表，否则语义会乱。

5) 一句话结论
你现在要的是：
“模型定义扩展（Meta）”和“具体表单扩展（Form Snapshot）”双轨并行，尤其表单级扩展要存完整快照并支持回滚。

你要的话我下一步可以直接给你：

表单级三张表字段清单（可直接建表）
full_snapshot_json 与 overlay_json 的最小必填字段规范。

是的，可以直接基于 md_form_extension 继续实现，这是最合适的，不需要另起炉灶。

我建议这样落：

怎么用 md_form_extension
md_form_extension 继续作为“表单级扩展入口”
扩展内容从“零散字段”升级为两部分：
overlay_json：加减字段、改属性、业务逻辑 patch（增量）
snapshot_json：应用扩展后的完整表单快照（你强调的状态）
每次保存扩展时：
先读取当前基线（原表单 + 上级扩展）
生成 before_snapshot
应用 overlay 生成 after_snapshot
存回 md_form_extension
并写历史表（建议加 md_form_extension_history）
你要的“继承来源”怎么记录
在 md_form_extension 增加字段（或等价字段）：

vendor_code（哪个厂商）
base_vendor_code（从哪个厂商扩展）
base_version / base_ref（从哪个版本扩展）
为什么这样好
不改客户接入方式
兼容你们已有逻辑
后续做页面（版本、diff、回滚）有数据基础
所以结论：用 md_form_extension 是对的，在现有逻辑上加 overlay + snapshot + history 三件套就行。
如果你同意，我下一步给你一版“最小改表方案（字段级）+ 保存流程时序图（文字版）”。


好，下面给你“最小改造方案”（基于 md_form_extension，不推翻现有逻辑）。

一、最小改表方案
在 md_form_extension 增加字段（建议）：

vendor_code：当前扩展所属厂商
base_vendor_code：继承来源厂商
base_ref：继承来源版本标识（如 vendor:VENDOR_A:ver:3）
overlay_json：本次增量扩展（加减字段/业务逻辑 patch）
snapshot_json：应用后完整快照（完整表单元数据）
snapshot_hash：快照摘要（幂等/对比）
ext_version：扩展版本号（乐观锁）
publish_status：draft/published/archived
published_at、published_by
若表已存在 ext_json 一类字段，可复用，不必全新字段名。

二、历史表（建议新增）
md_form_extension_history：

id
form_id
tenant_id
extension_id（指向 md_form_extension）
op_type：create/update/publish/rollback
before_snapshot_json（完整）
after_snapshot_json（完整）
overlay_json
operator
created_at
remark
三、保存流程（你关心的核心）
读取当前基线快照：
优先已发布 snapshot_json
没有则从当前表单元数据生成
记录 before_snapshot
应用 overlay_json
add/remove/update 字段
biz logic patch
生成 after_snapshot
更新 md_form_extension.snapshot_json
写 md_form_extension_history
四、发布/回滚流程
发布
仅 draft -> published
置 publish_status=published
记录发布人和时间
回滚
从 history 选一个 after_snapshot_json
复制为新 draft（新版本）
发布该 draft（不覆盖旧记录）
五、overlay_json 最小结构（建议）
Json

Apply
{
  "entityOps": {
    "addFields": [],
    "removeFields": [],
    "updateFields": []
  },
  "uiOps": {
    "addControls": [],
    "removeControls": [],
    "updateControls": []
  },
  "bizLogicOps": {
    "validators": [],
    "beforeSave": [],
    "operations": []
  }
}
六、snapshot_json 最小结构（建议）
存完整可回放结构，至少包含：

ui
formmeta
entitymeta（entity + table）
formExtension（业务逻辑块）
meta（vendorCode/baseRef/operator/time）
如果你同意，我下一步就按这套去落到后端实现（先字段兼容 + 保存链路写 snapshot/history）。